import 'package:flutter/material.dart';

class ReminderPrompt extends StatelessWidget {
  const ReminderPrompt({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
